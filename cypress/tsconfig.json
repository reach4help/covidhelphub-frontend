{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    // to avoid clashing with Jest types
    "types": ["cypress"]
  },
  "include": [
    "../node_modules/cypress",
    "./**/*.ts"
  ]
}