import styled from 'styled-components';

export const InputContainer = styled.label``;

export const InputLable = styled.span`
  font-family: ${(props: any) => props.theme.fontType};
  font-size: 40px;
  font-style: normal;
  font-weight: 400;
  line-height: 24px;
  letter-spacing: 0px;
  text-align: left;
  padding-left: 3px;
`;
